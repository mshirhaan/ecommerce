<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Document</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>

    <div class="topnav">
      <a class="<%= path == '/products' ? 'active' : '' %>" href="/products"
        >Products</a
      >
      <% if(isAuthenticated) { %>
      <a class="<%= path == '/add-product' ? 'active' : '' %>"
        
        href="/add-product"
        >Add Product</a
      >
      <% } %>

      <% if(!isAuthenticated) { %>
        <a class="<%= path == '/login' ? 'active' : '' %>" href="/login">Login</a>
        <% } %> 
  
        <% if(!isAuthenticated) { %>
          <a class="<%= path == '/signup' ? 'active' : '' %>"  href="/signup">Sign Up</a>
          <% } %> 
        <% if(isAuthenticated) { %>
        <a  href="/logout"
          >Logout</a
        >
        <% } %> 
        <p style="color: white;"><%= email %></p>
      </div>